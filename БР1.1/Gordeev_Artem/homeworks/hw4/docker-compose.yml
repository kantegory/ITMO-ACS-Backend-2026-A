services:
  swagger-ui:
    image: swaggerapi/swagger-ui:latest
    container_name: hw4_swagger_ui
    ports:
      - "3001:8080"
    environment:
      SWAGGER_JSON: /openapi.yaml
    volumes:
      - ./internal-openapi.yaml:/openapi.yaml:ro
