services:
  swagger-ui:
    image: swaggerapi/swagger-ui:latest
    container_name: job_board_swagger
    ports:
      - "8080:8080"
    volumes:
      - ./openapi.yaml:/usr/share/nginx/html/openapi.yaml
    environment:
      - URL=openapi.yaml