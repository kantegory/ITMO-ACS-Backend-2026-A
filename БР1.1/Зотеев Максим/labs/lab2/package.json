{
  "name": "rental-monorepo",
  "private": true,
  "workspaces": [
    "packages/*",
    "services/*"
  ],
  "scripts": {
    "build:shared": "npm run build -w @rental/shared",
    "build:all": "npm run build --workspaces --if-present"
  }
}
