spring:
  datasource:
    url: jdbc:postgresql://postgres-review:5432/reviews_db

  kafka:
    bootstrap-servers: kafka:9092

eureka:
  client:
    service-url:
      defaultZone: http://eureka-server:8081/eureka