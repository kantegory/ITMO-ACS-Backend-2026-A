spring:
  datasource:
    url: jdbc:postgresql://postgres-property:5432/property_db

  kafka:
    bootstrap-servers: kafka:9092

eureka:
  client:
    service-url:
      defaultZone: http://eureka-server:8081/eureka