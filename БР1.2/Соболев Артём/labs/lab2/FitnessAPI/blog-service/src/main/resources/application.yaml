spring:
  config:
    import: optional:classpath:common-lib-application.yaml
  application:
    name: blog-service
  datasource:
    url: ${BLOG_SERVICE_DB_URL:jdbc:postgresql://localhost:5432/blog_service}
    username: ${BLOG_SERVICE_DB_USERNAME:${DB_USERNAME:postgres}}
    password: ${BLOG_SERVICE_DB_PASSWORD:${DB_PASSWORD:postgres}}
    driver-class-name: org.postgresql.Driver
  rabbitmq:
    host: ${RABBITMQ_HOST:localhost}
    port: ${RABBITMQ_PORT:5672}
    username: ${RABBITMQ_USERNAME:fitness}
    password: ${RABBITMQ_PASSWORD:fitness}
  liquibase:
    change-log: classpath:/db/changelog/db.changelog-master.xml

app:
  integration:
    media-service:
      base-url: ${MEDIA_SERVICE_BASE_URL:http://media-service:8080/api/v1}
