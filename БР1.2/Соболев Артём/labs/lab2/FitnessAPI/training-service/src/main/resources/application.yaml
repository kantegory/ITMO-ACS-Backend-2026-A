spring:
  config:
    import: optional:classpath:common-lib-application.yaml
  application:
    name: training-service
  datasource:
    url: ${TRAINING_SERVICE_DB_URL:jdbc:postgresql://localhost:5432/training_service}
    username: ${TRAINING_SERVICE_DB_USERNAME:${DB_USERNAME:postgres}}
    password: ${TRAINING_SERVICE_DB_PASSWORD:${DB_PASSWORD:postgres}}
    driver-class-name: org.postgresql.Driver
  rabbitmq:
    host: ${RABBITMQ_HOST:localhost}
    port: ${RABBITMQ_PORT:5672}
    username: ${RABBITMQ_USERNAME:fitness}
    password: ${RABBITMQ_PASSWORD:fitness}
  liquibase:
    change-log: classpath:/db/changelog/db.changelog-master.xml

app:
  integration:
    media-service:
      base-url: ${MEDIA_SERVICE_BASE_URL:http://media-service:8080/api/v1}
