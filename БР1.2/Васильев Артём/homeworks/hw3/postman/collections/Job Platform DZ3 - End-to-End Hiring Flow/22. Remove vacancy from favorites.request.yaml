$kind: http-request
url: "{{baseUrl}}/favorites/{{vacancyId}}"
method: DELETE
auth:
  type: bearer
  credentials:
    token: "{{applicantToken}}"
scripts:
  - type: afterResponse
    code: pm.test('204 and empty response after favorite delete', function () {
      pm.response.to.have.status(204); pm.expect(pm.response.text()).to.eql('');
      });
    language: text/javascript
order: 22000
