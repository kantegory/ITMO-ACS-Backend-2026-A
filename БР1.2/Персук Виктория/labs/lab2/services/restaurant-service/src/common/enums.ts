export enum RoleName {
    Admin = 'Admin',
    User = 'User',
    Owner = 'Owner',
    Manager = 'Manager',
}

export enum RestaurantStatus {
    Pending = 'Pending',
    Verified = 'Verified',
    Rejected = 'Rejected',
}

export enum PriceCategory {
    Low = '$',
    Medium = '$$',
    High = '$$$',
}
