# Отчет по выполнению заданий

## ДЗ2: Проектирование и технический дизайн API

Сделано:
1. Сформулированы функциональные и нефункциональные требования в `docs/requirements.md`.
2. Выбран формат **REST API**.
3. Подготовлена спецификация `OpenAPI 3.0` в `docs/openapi.yaml`.
4. Для каждого endpoint описаны:
   - request body
   - response body
   - типовые ошибки (`400`, `401`, `404`, `409`).

## ЛР1: Реализация REST API на основе boilerplate

Сделано:
1. Доработаны модели:
   - `User` (email unique, `password_hash`)
   - `Restaurant` (кухня, город, адрес, средний чек, описание)
   - `Reservation` (статус)
   - добавлены `MenuItem`, `RestaurantPhoto`, `Review`
2. Добавлены связи между сущностями в `models/index.js`.
3. Реализованы контроллеры и маршруты:
   - регистрация и логин
   - профиль пользователя
   - история бронирований
   - поиск ресторанов с фильтрами
   - детальная страница ресторана (меню/фото/отзывы)
   - создание бронирования
4. Добавлена JWT-аутентификация (`middleware/auth.js`).
5. Добавлен seed-скрипт (`database/seed.js`) и npm-скрипты `start`, `seed`.

## ДЗ3: Тестирование API в Postman

Сделано:
1. Подготовлена коллекция:
   - `postman/restaurant-booking-main-flow.postman_collection.json`
2. В коллекции реализован единый сценарий:
   - регистрация -> вход -> список ресторанов -> фильтрация -> просмотр ресторана -> создание брони -> история броней.
3. На каждом запросе добавлены тесты (`pm.test`) на:
   - статус-коды
   - обязательные поля ответа
   - перенос данных между шагами через variables.
