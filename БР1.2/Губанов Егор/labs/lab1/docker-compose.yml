services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: rent
      POSTGRES_PASSWORD: rent
      POSTGRES_DB: rent
    ports:
      - "5432:5432"
    volumes:
      - rent_pg:/var/lib/postgresql/data

volumes:
  rent_pg:
