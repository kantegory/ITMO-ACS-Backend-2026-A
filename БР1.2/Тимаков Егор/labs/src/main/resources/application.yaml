spring:
  application:
    name: RentService
  datasource:
    url: jdbc:postgresql://localhost:5432/rent_service
    username: 
    password: 
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true

app:
  jwt:
    secret: ${JWT_SECRET:8PsTQh7t29YSlzijZMmFGSl1hZMeGUxgOTGghRNLv6B}
    expiration: ${JWT_EXPIRATION:86400000}

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html

server:
  port: 8080


